name: CI

on:
  push:
  pull_request:
  # Lets the release workflow run this same gate as a required job before
  # publishing (see release.yml).
  workflow_call:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
      - uses: extractions/setup-just@v4
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_dev.txt
          gem install mdl
      # Single gate, identical to the local pre-push hook (`just install-hooks`).
      - name: Format, lint, type-check, complexity, test, readme lint
        run: just check
