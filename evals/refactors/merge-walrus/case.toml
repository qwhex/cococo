kind = "merge_nested_if"
entry = "first_match"
expected_min_reduction = 2
detect = true
autofixable = false
source = "EX-A2-9 (walrus-gated nested if -> single and-condition)"
notes = "bad=6, good=4. Inner if nesting penalty (points=3, base=1) => reduction=2."
