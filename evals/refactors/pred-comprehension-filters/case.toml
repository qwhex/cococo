kind = "extract_predicate"
entry = "big_sales"
expected_min_reduction = 1
detect = false
forbidden_kind = "extract_predicate"
source = "catalog EX-A2-2"
notes = "delta is only 1 point (below MIN_REDUCTION=2, the deliberate noise floor) — surfacing it needs a scoring change (see docs/proposed-scoring-changes.md), not a detector. `detect = false` + `forbidden_kind` asserts the suppression instead: if a scoring change lifts this above the floor, precision fails and the case gets promoted to `detect = true`."
