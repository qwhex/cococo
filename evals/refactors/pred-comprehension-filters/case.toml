kind = "extract_predicate"
entry = "big_sales"
expected_min_reduction = 1
detect = false
source = "catalog EX-A2-2"
notes = "sequential if-filter syntax scores as comprehension-if (+2), not bool-op; engine stays silent but collapsing into a named predicate reduces score by 1"
