kind = "guard_clause"
entry = "find_admin"
expected_min_reduction = 1
detect = false
autofixable = false
behavior = ["find_admin([]) is None", "find_admin([type('U', (), {'role': 'user'})()] ) is None", "getattr(find_admin([type('U', (), {'role': 'admin'})()] ), 'role', None)"]
source = "EX-A2-7 (for/else where loop exits via return, not break -> drop cosmetic else)"
notes = "C8 flatten_else_after_return is not yet implemented; the for/else costs +1 nesting penalty on the for-node (bad=4, good=3). detect=false. autofixable=false: fix_source does not handle for/else."
