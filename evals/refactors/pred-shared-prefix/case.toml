kind = "extract_predicate"
entry = "process"
expected_min_reduction = 1
detect = false
source = "catalog EX-A5-8"
notes = "delta is only 1 point (below MIN_REDUCTION=2, the deliberate noise floor) — surfacing it needs a scoring change (see docs/proposed-scoring-changes.md), not a detector. Kept as reduction-only data."
