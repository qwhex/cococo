kind = "split_dispatcher"
entry = "get_storage_backend"
expected_min_reduction = 4
detect = true
source = "catalog EX-A1-10"
notes = "sequential if ladder; factory-function pattern preserves lazy env-var reads; detected by sequential_dispatch"
