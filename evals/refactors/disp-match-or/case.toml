kind = "split_dispatcher"
entry = "http_label"
expected_min_reduction = 1
detect = false
forbidden_kind = "split_dispatcher"
behavior = ["http_label(200)", "http_label(404)", "http_label(999)"]
source = "catalog EX-A2-3"
notes = "match with OR-pattern arms over int literals. A flat match costs +1 total, so the honest delta is 1 point (below MIN_REDUCTION=2, the deliberate noise floor) — the old cases-1 estimate over-stated it. `detect = false` + `forbidden_kind` asserts the suppression: if a scoring change lifts match above the floor, precision fails and the case gets promoted to `detect = true`. The positive nested-match path is pinned in tests/test_refactor.py."
