kind = "flatten_else_after_return"
entry = "process"
expected_min_reduction = 3
detect = true
autofixable = false
source = "flatten_else_after_return (drop redundant else after a terminal if)"
notes = "bad=7, good=4. else point (1) + for de-nest (1) + if de-nest (1) = reduction 3."
