kind = "split_dispatcher"
entry = "handle_state"
expected_min_reduction = 4
detect = false
source = "catalog EX-A5-9"
notes = "elif ladder keyed on enum members; engine does not detect non-literal keys; reduction validated only"
