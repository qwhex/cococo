"""Eval sets for cococo (not shipped with the library).

`refactors/` holds the refactor-suggestion eval set: per-case directories of real,
scoreable Python (`bad.py`/`good.py`) plus a `case.toml`. See `refactor_eval.py`
for the loader/grader and `tests/test_eval_refactors.py` for the gate.
"""
