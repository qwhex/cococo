# Lint + type-check — same toolchain as the parent cosmetix data_pipeline.
# Pinned exactly: CI installs these fresh on every run, and an unpinned ruff
# turned CI red with no repo change (0.16.0 began formatting Markdown code
# blocks). Bump deliberately, running `just check` with the new pin.
ruff==0.15.16
mypy==2.1.0

# Test suite
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100

# Eval set: case.toml parsing (tomllib is stdlib on 3.11+).
tomli>=2.0; python_version < "3.11"

# Build + publish (see RELEASING.md). Used by `just build` / `just publish`;
# CI publishes via trusted publishing, so these are only needed for local builds.
build>=1.2
twine>=5.1
